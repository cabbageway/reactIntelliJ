import React from 'react';
import {IName} from "../models/Interfaces";

function Gernot({name}: IName) {
    return (
        <div>
            <h1>aus Gernot {name}</h1>
            <h1>aus Gernot {name}</h1>
        </div>
    );
}

export default Gernot;